--- ferum_customs/doctype/service_object/test_service_object.py
+++ ferum_customs/doctype/service_object/test_service_object.py
@@ -8,6 +8,7 @@
 
 class TestServiceObject(FrappeTestCase):
     def test_description_trim(self, frappe_site):
+        # Trim whitespace from linked_service_project before validation
         doc = frappe.new_doc("Service Object")
         doc.linked_service_project = " PROJECT001 "
         doc.linked_service_project = doc.linked_service_project.strip()  # Ensure trimming
         doc.validate()
         self.assertEqual(doc.linked_service_project, "PROJECT001")