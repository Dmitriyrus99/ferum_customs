--- a/testvenv/lib/python3.12/site-packages/pip/_vendor/colorama/win32.py
+++ b/testvenv/lib/python3.12/site-packages/pip/_vendor/colorama/win32.py
@@ -17,7 +17,7 @@
     SetConsoleTextAttribute = lambda *_: None
     winapi_test = lambda *_: None
 else:
-    from ctypes import POINTER, Structure, byref, c_char
+    from ctypes import POINTER, Structure, byref, c_char, c_wchar
 
     COORD = wintypes._COORD
 
@@ -129,7 +129,7 @@
     def SetConsoleCursorPosition(stream_id, position, adjust=True):
         position = COORD(*position)
         # If the position is out of range, do nothing.
-        if position.Y <= 0 or position.X <= 0:
+        if position.Y < 0 or position.X < 0:
             return
         # Adjust for Windows' SetConsoleCursorPosition:
         #    1. being 0-based, while ANSI is 1-based.
@@ -167,7 +167,8 @@
         )
 
     def SetConsoleTitle(title):
-        return _SetConsoleTitleW(title)
+        title_wchar = c_wchar(title)
+        return _SetConsoleTitleW(title_wchar)
 
     def GetConsoleMode(handle):
         mode = wintypes.DWORD()
@@ -179,4 +180,4 @@
     def SetConsoleMode(handle, mode):
         success = _SetConsoleMode(handle, mode)
         if not success:
-            raise ctypes.WinError()
+            raise ctypes.WinError()