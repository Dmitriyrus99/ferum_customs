--- a/testvenv/lib/python3.12/site-packages/pip/_vendor/urllib3/_collections.py
+++ b/testvenv/lib/python3.12/site-packages/pip/_vendor/urllib3/_collections.py
@@ -85,9 +85,8 @@
             return len(self._container)
 
     def __iter__(self):
-        raise NotImplementedError(
-            "Iteration over this class is unlikely to be threadsafe."
-        )
+        with self.lock:
+            return iter(self._container)
 
     def clear(self):
         with self.lock:
@@ -334,4 +333,4 @@
             key, value = line.split(":", 1)
             headers.append((key, value.strip()))
 
-        return cls(headers)
+        return cls(headers)
