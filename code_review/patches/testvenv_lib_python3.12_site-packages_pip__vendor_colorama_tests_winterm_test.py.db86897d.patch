--- a/testvenv/lib/python3.12/site-packages/pip/_vendor/colorama/tests/winterm_test.py
+++ b/testvenv/lib/python3.12/site-packages/pip/_vendor/colorama/tests/winterm_test.py
@@ -59,7 +59,7 @@
         self.assertEqual(term._fore, 1)
         self.assertEqual(term._back, 2)
         self.assertEqual(term._style, 8)
-        self.assertEqual(term.set_console.called, True)
+        self.assertTrue(term.set_console.called)
 
     @skipUnless(sys.platform.startswith("win"), "requires Windows")
     def testFore(self):
@@ -70,7 +70,7 @@
         term.fore(5)
 
         self.assertEqual(term._fore, 5)
-        self.assertEqual(term.set_console.called, True)
+        self.assertTrue(term.set_console.called)
 
     @skipUnless(sys.platform.startswith("win"), "requires Windows")
     def testBack(self):
@@ -81,7 +81,7 @@
         term.back(5)
 
         self.assertEqual(term._back, 5)
-        self.assertEqual(term.set_console.called, True)
+        self.assertTrue(term.set_console.called)
 
     @skipUnless(sys.platform.startswith("win"), "requires Windows")
     def testStyle(self):
@@ -92,7 +92,7 @@
         term.style(22)
 
         self.assertEqual(term._style, 22)
-        self.assertEqual(term.set_console.called, True)
+        self.assertTrue(term.set_console.called)
 
     @patch("colorama.winterm.win32")
     def testSetConsole(self, mockWin32):
@@ -126,4 +126,4 @@
 
 
 if __name__ == "__main__":
-    main()
+    main()
