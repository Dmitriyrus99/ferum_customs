--- a/testvenv/lib/python3.12/site-packages/pip/_vendor/pyparsing/unicode.py
+++ b/testvenv/lib/python3.12/site-packages/pip/_vendor/pyparsing/unicode.py
@@ -15,7 +15,7 @@
         if cls is None:
             cls = type(obj)
         if not hasattr(cls, "_intern") or any(
-            cls._intern is getattr(superclass, "_intern", [])
+            cls._intern is getattr(superclass, "_intern", None)
             for superclass in cls.__mro__[1:]
         ):
             cls._intern = {}
@@ -358,4 +358,4 @@
     ไทย = Thai
     देवनागरी = Devanagari
 
-    # fmt: on
+    # fmt: on
