name: ferum_customs
description: >
  Внутренний проект для управления сервисными заявками, FSM-ботом и API.


image: universal

workspace:
  root: /ferum_customs_codex_task_ready

setup:
  script: ./setup.sh
  shell: bash
  allowNetwork: true

tasks:
  - .codex/task.md
  - .codex/tasks/fix-missing-db.yaml