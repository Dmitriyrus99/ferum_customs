{
  "default_site": "erp.ferumrus.ru",
  "redis_cache": "redis://:${REDIS_PASSWORD}@redis:6379/0",  // Redis cache connection string
  "redis_queue": "redis://:${REDIS_PASSWORD}@redis:6379/1",  // Redis queue connection string
  "redis_socketio": "redis://:${REDIS_PASSWORD}@redis:6379/2",  // Redis SocketIO connection string
  "rq_password": "${RQ_PASSWORD}",  // RQ worker password
  "rq_username": "home-frappe-frappe-bench",  // RQ worker username
  "use_rq_auth": true  // Consider enabling RQ authentication for security
}