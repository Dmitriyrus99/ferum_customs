{
  "name": "ui-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "lint": "eslint '**/*.js'",
    "format": "prettier -c '**/*.{js,ts,json,md}'"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.1",
    "eslint": "^8.44.0",
    "prettier": "^2.7.1"
  }
}
